<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.ontio.explorer.statistics.mapper.NodeOverviewMapper">

    <update id="updateBlkCntToNxtRnd" parameterType="java.lang.Long">
        UPDATE tbl_node_overview
        SET blk_cnt_to_nxt_rnd = #{blkCntToNxtRnd}
        WHERE id = 1;
    </update>

    <update id="updateRoundStartTime" parameterType="java.lang.Integer">
        UPDATE tbl_node_overview
        SET rnd_start_time = #{roundStartTime}
        WHERE id = 1;
    </update>

    <select id="selectBlkCountToNxtRnd" resultType="java.lang.Long" useCache="true">
        SELECT blk_cnt_to_nxt_rnd
        FROM tbl_node_overview
        WHERE id = 1;
    </select>

    <update id="updateLeftTimeToNxtRnd" parameterType="java.lang.Long">
        UPDATE tbl_node_overview
        SET left_time_to_next_rnd = #{leftTimeToNextRnd}
        WHERE id = 1;
    </update>


</mapper>